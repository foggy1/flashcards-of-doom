<h1><%= @deck.title %></h1>

<p>You've completed a round of <%= @deck.title %>. You answered <%= @correct_guesses.count %>/<%= @deck.cards.count %> cards correctly on the first try. You submitted <%= @guess_ids.count %> total guesses before each card was answered correctly.</p>